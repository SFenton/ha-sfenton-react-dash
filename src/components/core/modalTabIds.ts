export function modalTabId(idPrefix: string, tab: string) {
  return `${idPrefix}-tab-${tab}`
}

export function modalTabPanelId(idPrefix: string, tab: string) {
  return `${idPrefix}-panel-${tab}`
}
